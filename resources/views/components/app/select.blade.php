<div class="bootstrap-select">
    <select name="{{ $name }}"
            class="selectpicker"
            data-live-search="true"
            data-header="{{ $header }}">
        {{ $slot }}
    </select>
</div>